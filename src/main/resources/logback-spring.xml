<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Setting up log path and log file name -->
    <property name="LOG_PATH" value="./logs" />
    <property name="LOG_FILE_NAME" value="app" />

    <!-- Setting up logging pattern for console logging -->
    <appender name="ConsoleOutput"
              class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%yellow(%t)] %magenta(%C{1}): %msg%n%throwable
            </Pattern>
        </encoder>
    </appender>

    <!-- Setting the filename and logging pattern for log file -->
    <appender name="LogFile"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %p [%t] %C{1}: %m%n
            </Pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- ASYNC FILE (bonus performance) -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="LogFile"/>
        <queueSize>512</queueSize>
    </appender>

    <!-- Root Logger -->
    <root level="info">
        <appender-ref ref="ASYNC_FILE" />
        <appender-ref ref="ConsoleOutput" />
    </root>

    <!-- Application Logger -->
    <logger name="com.logging" level="trace" additivity="false">
        <appender-ref ref="ASYNC_FILE" />
        <appender-ref ref="ConsoleOutput" />
    </logger>

</configuration>